/*
p = mat + 1 -> Válido; p recebe o endereço do segundo elemento do vetor
p = mat -> Válido; p recebe o endereço do primeiro elemento do vetor
p = `mat -> Inválido; o sinal “`” não representa nada em C
x = (*mat) -> Válida; embora “mat” não tenha recebido valor pelo usuário, o próprio programa atribui um valor automaticamente. */